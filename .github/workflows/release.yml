name: Release and Publish

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22' # Node 22 includes npm v10.8+ with provenance support
          registry-url: 'https://registry.npmjs.org'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test:run

      - name: Type check
        run: pnpm type-check

      - name: Build
        run: pnpm build

      - name: Update package version
        run: |
          # Extract version from GitHub Release tag (e.g., v1.0.0 -> 1.0.0)
          VERSION="${GITHUB_REF#refs/tags/v}"
          echo "Publishing version: $VERSION"

          # Update package.json version
          npm version $VERSION --no-git-tag-version --allow-same-version
        env:
          GITHUB_REF: ${{ github.ref }}

      - name: Publish to npm (using npm for OIDC Trusted Publishing)
        run: npm publish --provenance --access public
        # Uses OIDC authentication via id-token: write permission
        # No NODE_AUTH_TOKEN needed - secure short-lived tokens only
